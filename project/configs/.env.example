# FastAPI / Uvicorn

HOST=127.0.0.1
PORT=8000

# Detection settings

DEFAULT_SCORE_THRESHOLD=0.3

# Paths

MODEL_PATH = artifacts/models/best_detector.pt
META_PATH = artifacts/metrics/best_detector_meta.json

# Optional

DEVICE = auto